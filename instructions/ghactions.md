# Інструкція з налаштування GitHub Actions для розгортання Vite на GitHub Pages

Ця інструкція крок за кроком пояснює, як автоматизувати процес деплою вашого React-додатку (створеного за допомогою Vite) на GitHub Pages використовуючи GitHub Actions.

## Крок 1: Підготовка конфігурації Vite

Перш ніж налаштовувати GitHub Actions, необхідно вказати базовий шлях (base path) у вашому проекті. Це потрібно, щоб браузер правильно знаходив CSS та JS файли на GitHub Pages.

1.  Відкрийте файл `vite.config.js` у корені вашого проекту.
2.  Додайте властивість `base` з назвою вашого репозиторія.

**Приклад `vite.config.js`:**

```javascript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  // ВАЖЛИВО: замініть 'repo-name' на назву вашого репозиторія
  // Наприклад, якщо репозиторій https://github.com/user/my-app, то base має бути '/my-app/'
  base: '/repo-name/', 
})
```

## Крок 2: Створення файлу Workflow

GitHub Actions використовує спеціальні YAML-файли ("workflows"), щоб знати, що і коли робити.

1.  У корені проекту створіть папку `.github` (з крапкою на початку).
2.  В ній створіть папку `workflows`.
3.  Всередині `workflows` створіть файл з назвою `deploy.yml`.

Повний шлях має виглядати так: `.github/workflows/deploy.yml`

Скопіюйте наступний код у цей файл:

```yaml
# Назва workflow
name: Deploy static content to Pages

on:
  # Запускати цей workflow при кожному push'і в гілку master
  # ВАЖЛИВО: Перевірте назву вашої основної гілки (main або master)
  push:
    branches: ['master']

  # Дозволяє запускати цей workflow вручну через вкладку Actions
  workflow_dispatch:

# Права доступу, необхідні для деплою на GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Дозволяє тільки один процес деплою одночасно
concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  # Задача деплою
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      # 1. Завантажуємо код з репозиторія
      - name: Checkout
        uses: actions/checkout@v4

      # 2. Встановлюємо Node.js
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      # 3. Встановлюємо залежності (npm install)
      - name: Install dependencies
        run: npm ci

      # 4. Збираємо проект (npm run build)
      - name: Build
        run: npm run build

      # 5. Налаштовуємо Pages
      - name: Setup Pages
        uses: actions/configure-pages@v5

      # 6. Завантажуємо папку dist (результат збірки) як артефакт
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Вказуємо шлях до папки, яку створив Vite (зазвичай dist)
          path: './dist'

      # 7. Деплоїмо артефакт на GitHub Pages
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

## Крок 3: Збереження змін (Git)

Тепер, коли файли створено, їх потрібно відправити на GitHub.

Відкрийте термінал і виконайте команди:

```bash
git add .
git commit -m "Configure GitHub Actions deployment"
git push origin master
```
*(Якщо ваша гілка називається `main`, замініть `master` на `main`).*

## Крок 4: Налаштування репозиторія на GitHub

Це критично важливий крок. GitHub за замовчуванням може очікувати деплой з іншої гілки (наприклад, gh-pages), а ми хочемо використовувати **Actions**.

1.  Відкрийте ваш репозиторій на GitHub.
2.  Перейдіть на вкладку **Settings** (Налаштування).
3.  У меню зліва знайдіть розділ **Pages** (у блоці "Code and automation").
4.  У розділі **Build and deployment**:
    *   Знайдіть пункт **Source**.
    *   У випадаючому списку виберіть **GitHub Actions**.

## Крок 5: Перевірка роботи

1.  Після того, як ви зробили `push` і налаштували сторінку на GitHub, перейдіть на вкладку **Actions** у вашому репозиторії.
2.  Ви маєте побачити процес з назвою "Deploy static content to Pages" (або вашим останнім коміт-повідомленням).
    *   🟡 Жовтий: Процес виконується.
    *   ✅ Зелений: Успішно завершено.
    *   ❌ Червоний: Помилка (натисніть, щоб подивитися деталі).
3.  Якщо все зелене, поверніться в **Settings -> Pages**. Вгорі ви побачите посилання на ваш опублікований сайт (наприклад, `https://username.github.io/repo-name/`).

## Вирішення проблем

*   **Workflow не запускається після push**:
    *   Перевірте, чи правильно вказана гілка у файлі `deploy.yml` (`branches: ['master']` vs `branches: ['main']`).
    *   Перевірте, чи ви дійсно зробили `push` на GitHub.

*   **Сайт відкривається, але екран білий / нічого не працює**:
    *   Відкрийте консоль браузера (F12). Якщо бачите багато помилок `404 Not Found` для файлів `.js` та `.css` — ви неправильно налаштували `base` у `vite.config.js`. Перевірте Крок 1.

*   **Помилка "npm ci" failed**:
    *   Переконайтеся, що файл `package-lock.json` є у репозиторії. Команда `npm ci` вимагає його наявності для точного відтворення залежностей.
